overwrite: true
generates:
  graphql/authenticatedSchema.graphql:
    schema: http://localhost:4000/graphql
    plugins:
      - schema-ast

  graphql/anonymousSchema.graphql:
    schema: http://localhost:4000/graphql/anonymous
    plugins:
      - schema-ast
